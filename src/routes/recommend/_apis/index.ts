// The recommend feature's API surface — the resource lives in the shared module
// (#/libs/api/recommend.ts).

export type { TPreferences, TRecommendRequest, TRecommendResponse } from "#/libs/api/recommend.ts"
export { recommend } from "#/libs/api/recommend.ts"
